List:
? for my $item(@{ $_[0]->{data} }) {
    * <?= $item->{title} ?>
    * <?= $item->{title} ?>
    * <?= $item->{title} ?>
? }
List:
? for my $item(@{ $_[0]->{data} }) {
    * <?= $item->{title} ?>
    * <?= $item->{title} ?>
    * <?= $item->{title} ?>
? }
List:
? for my $item(@{ $_[0]->{data} }) {
    * <?= $item->{title} ?>
    * <?= $item->{title} ?>
    * <?= $item->{title} ?>
? }
